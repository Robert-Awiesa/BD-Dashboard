{
  "name": "bd-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "BD Workspace Dashboard — Express API + Vite frontend, deployed as one Render web service.",
  "scripts": {
    "build": "cd Server && npm install && cd ../workspace && npm install && npm run build && cp -r dist ../Server/dist",
    "start": "cd Server && npm start"
  }
}
